<%= form_for [@movie, @review] do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>

  <div class="field">
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit "Save", id: "btn"  %>
  </div>
<% end %>
