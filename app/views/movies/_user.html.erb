<p id="notice"><%= notice %></p>
<div id="font">
<h3>Favorite Movies</h3>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <!-- <th>Plot</th> -->
      <!-- <th>Year</th>
      <th>Genre</th>
      <th>Time</th>
      <th>Url</th>
      <th>Rating</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= image_tag movie.posters.first.file("medium")%></td>
        <td><%= movie.title %></td>
        <!--<td>|<%= movie.plot %></td> 
        <td><%= movie.year %></td>
        <td><%= movie.genre %></td>
        <td><%= movie.time %></td>
        <td><%= movie.url %></td>
        <td><%= movie.rating %></td> -->
        <td><%= link_to 'Show', movie %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>